import 'package:flutter/material.dart';

class ViewStoryView extends StatelessWidget {
  const ViewStoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
