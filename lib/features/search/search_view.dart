import 'package:flutter/material.dart';

class SearchViw extends StatelessWidget {
  const SearchViw({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Chats'),
      ),
    );
  }
}
